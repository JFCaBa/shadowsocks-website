{{ printf "<?xml version=\"1.0\" encoding=\"utf-8\" standalone=\"yes\"?>" | safeHTML }}
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9"
  xmlns:xhtml="http://www.w3.org/1999/xhtml">
  {{- range .Data.Pages }}
  <url>
    <loc>{{ .Permalink }}</loc>
    {{- if not .Lastmod.IsZero }}
    <lastmod>{{ .Lastmod.Format "2006-01-02" }}</lastmod>
    {{- end }}
    {{- if .IsHome }}
    <changefreq>weekly</changefreq>
    <priority>1.0</priority>
    {{- else if eq .Section "guides" }}
    <changefreq>monthly</changefreq>
    <priority>0.9</priority>
    {{- else if or (eq .RelPermalink (printf "/%s/why-shadowsocks/" .Language.Lang)) (eq .RelPermalink (printf "/%s/pochemu-shadowsocks/" .Language.Lang)) (eq .RelPermalink (printf "/%s/por-que-shadowsocks/" .Language.Lang)) }}
    <changefreq>monthly</changefreq>
    <priority>0.8</priority>
    {{- else if or (eq .RelPermalink (printf "/%s/faq/" .Language.Lang)) }}
    <changefreq>monthly</changefreq>
    <priority>0.7</priority>
    {{- else }}
    <changefreq>monthly</changefreq>
    <priority>0.5</priority>
    {{- end }}
    {{- if .IsTranslated }}
    {{- range .Translations }}
    <xhtml:link
      rel="alternate"
      hreflang="{{ .Language.Lang }}"
      href="{{ .Permalink }}" />
    {{- end }}
    <xhtml:link
      rel="alternate"
      hreflang="{{ .Language.Lang }}"
      href="{{ .Permalink }}" />
    {{- end }}
  </url>
  {{- end }}
</urlset>
